# These are set automatically by Render when using render.yaml
# For local development, create backend/.env with these values:

DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost/marketdb
JWT_SECRET=your_super_secret_key_change_this
